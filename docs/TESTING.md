# Testing Checkpoints

Run the tests below after completing each session. A session is not done until its checkpoint passes.

---

## Session 1  -  MongoDB Atlas + Core Storage/Retrieval

**Script:** `python tests/test_session1.py`

Manual checks after script passes:
- [ ] Log into Atlas UI → collection `chunks` shows 5 documents
- [ ] Atlas Search index `vector_index` status shows "Active" (not "Building")
- [ ] Pre-filter by `metadata.event_name = "hacklanta"` in Atlas UI returns exactly 1 doc

**Pass criteria:** Script exits with "All Session 1 tests PASSED." No assertion errors.

---

## Session 2  -  Ingestion Pipeline

**Script:** `python tests/test_session2.py`

What the script must verify:
- [ ] `pii_filter.py`: email address is replaced with `[EMAIL]`, phone with `[PHONE]`, student ID with `[STUDENT_ID]`; exec names from `PII_RULES.md` are preserved
- [ ] `chunker.py`: meeting notes doc splits on date headers or `---`; event logistics splits on `##` headings; chunk count is > 1 for a multi-section doc
- [ ] `noise_filter.py`: a chunk with a concrete decision scores YES; a chunk of pure emoji/filler scores NO (requires `GEMINI_API_KEY`)
- [ ] `embedder.py`: output vector has exactly 768 dimensions; all values are floats
- [ ] `run_ingestion.py` end-to-end on 1 real file: chunk count logged, noise pass rate logged, no uncaught exceptions

Manual checks after script passes:
- [ ] Run ingestion on Priority 1 files from `DATA_MAP.md`; Atlas collection `chunks` count is 100 - 2000
- [ ] Spot-check 3 random chunks in Atlas: `metadata.file_title` is correct, `metadata.date` is not null for event docs, `embedding` array has 768 elements
- [ ] Grep Atlas for `[EMAIL]` or raw phone pattern  -  should return 0 results
- [ ] Run `check_chunk_for_pii()` from `PII_RULES.md` on 20 random chunks  -  0 violations

**Pass criteria:** Script exits clean. Atlas chunk count in range. 0 PII violations.

---

## Session 3  -  Retrieval Layer

**Script:** `python tests/test_session3.py`

What the script must verify:
- [ ] `vector_search.py` with real Atlas data returns 10 results for an unfiltered query
- [ ] `reranker.py`: given 10 chunks and a query, returns ≤ 3 results; chunks scoring >= 6 are kept, fallback keeps top 2 if fewer than 2 meet threshold; results are sorted descending by `rerank_score`
- [ ] `retriever.py`: single call returns ≤ 3 results, each has keys `text`, `metadata`, `rerank_score`

Manual checks  -  run all 3 demo queries from `DEMO_SCRIPT.md` via Python (not UI):
- [ ] Query 1 (Hacklanta logistics): top result `metadata.event_name == "hacklanta"`, at least 2 Hacklanta sources in top 3
- [ ] Query 2 (attendance growth): at least 1 result from `doc_type == "growth"` or aggregate file; answer includes numeric data
- [ ] Query 3 (hackathon planning brief): top results span at least 3 different `file_id`s

**Pass criteria:** Script exits clean. All 3 demo queries return relevant sources manually confirmed.

---

## Session 4  -  Agent + API

**Script:** `python tests/test_session4.py`

What the script must verify:
- [ ] `mode_classifier.py`: "What happened at Hacklanta?" → `RECALL`; "How has attendance grown?" → `ANALYZE`; "Draft a planning brief for..." → `PLAN`
- [ ] `POST /chat` with Query 1 returns JSON with keys `mode`, `answer`, `citations`
- [ ] `citations` is a non-empty list; each item has `source_name`, `date`, `drive_link`, `rerank_score`
- [ ] `POST /chat` with Query 3 returns `created_doc_url` that is non-null and starts with `https://docs.google.com`
- [ ] MongoDB MCP connection is live: agent tool call reaches Atlas (check server logs)

Manual checks:
- [ ] Open `created_doc_url` in browser  -  doc exists in Drive, has ≥ 4 sections, has "Generated by progsu Intelligence Agent" footer
- [ ] Confirm doc appears in the folder specified by `DRIVE_OUTPUT_FOLDER_ID` in `.env`
- [ ] All 3 demo queries return in under 8 seconds (time with `time curl ...`)
- [ ] Re-run same query twice  -  answers are consistent (not wildly different)

**Pass criteria:** Script exits clean. Google Doc exists and has correct content. All 3 queries under 8s.

---

## Session 5  -  Frontend

No automated script. Manual checklist only:

- [ ] `npm run dev` starts without errors; app loads at localhost
- [ ] Type Query 1 → RECALL badge appears (blue), answer streams in, citation cards render with doc title + date
- [ ] Clicking a citation card opens the correct Google Drive doc in a new tab
- [ ] Type Query 2 → ANALYZE badge appears (purple)
- [ ] Type Query 3 → PLAN badge appears (green), Google Doc link appears below the answer
- [ ] Clicking the Google Doc link opens the correct doc in Drive
- [ ] Mode badge (RECALL / ANALYZE / PLAN) displays correctly for each response - static legend only, no toggle or filter behavior
- [ ] No console errors in browser devtools during any of the above
- [ ] Test on a narrow viewport (375px)  -  chat is usable, not broken

**Pass criteria:** All checklist items checked. Zero console errors on demo queries.

---

## Session 6  -  Pre-Demo Final Check

Run before recording. Every item must be green.

- [ ] Run all 3 demo queries 5 times each  -  answers are consistent across all 5 runs
- [ ] Query 1 cites at least 2 Hacklanta sources every time
- [ ] Query 2 includes real attendance numbers (400+, 200+) every time
- [ ] Query 3 creates a new Google Doc every time (not reusing a cached one)
- [ ] All responses return in under 8 seconds on 5/5 runs
- [ ] Citation links all resolve (no 404s)
- [ ] Backend deployed; frontend deployed; demo uses live URLs, not localhost
- [ ] Screen recording setup tested: mode badge visible, citation cards visible, Drive doc opening is captured
- [ ] Run `check_chunk_for_pii()` on full Atlas collection  -  0 violations before submission

**Pass criteria:** Every box checked. Do not record until all pass.
